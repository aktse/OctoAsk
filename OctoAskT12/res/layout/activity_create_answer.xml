<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="cs.ualberta.octoaskt12.CreateAnswerActivity" >

    <EditText
        android:id="@+id/questionBodyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="34dp"
        android:background="@drawable/question_edit_text"
        android:ems="10"
        android:gravity="top"
        android:hint="Answer..."
        android:inputType="textMultiLine"
        android:minHeight="100dp"
        android:padding="10dp" >
    </EditText>

    <Button
        android:id="@+id/cancelQuestionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/questionBodyText"
        android:layout_below="@+id/questionBodyText"
        android:layout_marginTop="127dp"
        android:background="@drawable/question_button"
        android:padding="10dp"
        android:text="Cancel"
        android:textColor="#ffffff" />

    <Button
        android:id="@+id/submitQuestionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/cancelQuestionButton"
        android:layout_alignBottom="@+id/cancelQuestionButton"
        android:layout_alignRight="@+id/questionBodyText"
        android:background="@drawable/question_button"
        android:padding="10dp"
        android:text="Submit"
        android:textColor="#ffffff" />

</RelativeLayout>